
export enum TokenType {
  Plus = "+",
  Minus = "-",
  Multiply = "*",
  Divide = "/",
  LeftParen = "(",
  RightParen = ")",
  Number = "NUMBER",
  Name = "NAME",
  EOF = "EOF",
  If = "IF",
  Else = "ELSE",
  While = "WHILE",
  Then = "THEN",
  Do = "DO",
}
